# DeepSeek Harness 桌面应用 v0.1.0

把 [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness) 的 Web GUI 封装成 Windows 桌面应用。
宿主是 GUI 子系统，**全程没有终端窗口**，也不会出现"关掉黑窗服务就跟着退出"的问题。

## 下载哪个

| 文件 | 说明 |
| --- | --- |
| `DeepSeek Harness-0.1.0-x64.exe` | NSIS 向导安装包，**安装时可自定义安装目录**；卸载默认保留缓存（弹窗询问后才删） |
| `DeepSeek Harness-0.1.0-portable.exe` | 免安装单目录，放 U 盘即可运行 |

约 164MB。内置了 harness 运行时与 Node，**目标机器无需预装 Node.js**。

## 安装后

双击桌面快捷方式即可：服务没跑就先起服务，然后自动打开界面。
首次启动会把内置的 harness 解包到 `%APPDATA%\DeepSeekHarness\harness\`（约 213MB，需等待十几秒到半分钟）。

## 校验下载（可选）

本版本**未做代码签名**，Windows SmartScreen 可能提示"Windows 已保护你的电脑"——
点"更多信息 → 仍要运行"即可。也可以先核对哈希：

```
SHA256
757EA3D54AECB9E9FC812B5B34A6B85E347BD8A1EADEFC254317364AAB140184  DeepSeek Harness-0.1.0-x64.exe
805C3B2C50C8A11793BFA12E8E0768F21B9E9E89C7C3DFB29C70A92FAB500AAA  DeepSeek Harness-0.1.0-portable.exe
```

```powershell
Get-FileHash "DeepSeek Harness-0.1.0-x64.exe" -Algorithm SHA256
```

## 两条独立的更新通道

- **DeepSeek Harness 版本**：应用内「设置」里检查更新，装到独立版本目录并只重启 harness 服务；保留上一个版本可一键回滚。
- **应用本体**：通过本仓库的 Releases 更新。

## 架构要点

外壳只依赖 `dsh web` 这个稳定的 CLI 契约（`--port` / `--no-open` / 打印带 token 的登录地址），
不 import harness 内部实现——所以 harness 能独立升级、回滚，不需要重发应用。

## 系统要求

- Windows 10 1803+ / Windows 11（x64）
- 约 600MB 可用磁盘（两棵 harness 版本树）

## 说明

- 本项目为第三方封装，源码在 https://github.com/learnermake/deepseek-harness-app
- 随包分发的第三方组件许可证见应用内「文件 → 第三方许可证」
- 内置的 DeepSeek Harness（`@deepseek-ai/dsh`）为 MIT 许可，版权归 DeepSeek 所有
